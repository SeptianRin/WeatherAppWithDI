<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WeatherAppWithDI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WeatherAppWithDI" parent="Base.Theme.WeatherAppWithDI" />
    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/bg_btn</item> <!-- Background drawable -->
        <item name="android:textColor">@color/primary</item> <!-- Text color -->
        <!-- Add any other attributes you want to customize for the button -->
    </style>
</resources>