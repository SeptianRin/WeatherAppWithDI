<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.WeatherAppWithDI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WeatherAppWithDI" parent="Base.Theme.WeatherAppWithDI" />


    <style name="MyCustomTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize theme attributes here -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/darkBlue</item>
        <item name="colorButtonNormal">@color/darkBlue</item>
        <item name="textAllCaps">false</item>
        <item name="android:textColor">@color/primary</item>
        <!-- Add more customizations as needed -->
    </style>
</resources>